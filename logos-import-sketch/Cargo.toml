[package]
name = "logos-import-sketch"
version = "0.1.0"
edition = "2021"
description = "Sketch (.sketch) file format importer for Logos"

[dependencies]
logos-core = { path = "../logos-core" }
logos-import-common = { path = "../logos-import-common" }
thiserror = "2.0"
uuid = { version = "1.20", features = ["v4"] }
log = "0.4"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
flate2 = "1.1"

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "sketch_benchmark"
harness = false
