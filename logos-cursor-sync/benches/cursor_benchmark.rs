use criterion::{criterion_group, criterion_main, Criterion};

fn cursor_benchmarks(_c: &mut Criterion) {
    // Placeholder for future benchmarks
}

criterion_group!(benches, cursor_benchmarks);
criterion_main!(benches);
