use criterion::{criterion_group, criterion_main, Criterion};

fn prototyping_benchmarks(_c: &mut Criterion) {
    // Placeholder – benchmarks will be fleshed out later.
}

criterion_group!(benches, prototyping_benchmarks);
criterion_main!(benches);
