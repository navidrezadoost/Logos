use criterion::{criterion_group, criterion_main, Criterion};

fn auth_benchmarks(_c: &mut Criterion) {
    // Placeholder for auth benchmarks
}

criterion_group!(benches, auth_benchmarks);
criterion_main!(benches);
