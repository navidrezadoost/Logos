[package]
name = "logos-agent-rollback"
version = "0.1.0"
edition = "2021"
description = "Agent Version Rollback & A/B Testing for Logos — versioned snapshots, rollback engine, traffic splitting"

[dependencies]
thiserror = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
log = "0.4"
