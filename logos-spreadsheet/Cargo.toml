[package]
name = "logos-spreadsheet"
version = "0.3.0"
edition = "2021"
description = "Spreadsheet formula engine for Logos — parsing, evaluation, cell references, functions, arrays, and lookups"
license = "MPL-2.0"

[dependencies]
thiserror = "1"

[dev-dependencies]
