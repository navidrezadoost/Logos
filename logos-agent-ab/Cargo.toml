[package]
name = "logos-agent-ab"
version = "0.1.0"
edition = "2021"
description = "Agent A/B Testing Framework for Logos — traffic splitting, metric collection, and statistical significance testing"

[dependencies]
thiserror = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
log = "0.4"
