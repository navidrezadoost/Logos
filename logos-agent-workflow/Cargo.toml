[package]
name = "logos-agent-workflow"
version = "0.1.0"
edition = "2021"
description = "Agent Workflow Orchestrator for Logos — DAG execution engine, retry/timeout/backoff policies, and checkpointing"

[dependencies]
thiserror = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
log = "0.4"
