// logos-rebase — Phase 3 placeholder
//
// Rust port of `common/src/app/common/files/rebase.cljc`.
// The OT transform matrix lives in Clojure(Script) for Phase 2.
// This crate will be promoted to a full implementation in Phase 3 when
// the hot-path rebase cost becomes measurable.
//
// Expected API (to be implemented):
//   pub fn rebase_change(change: &Change, competing: &[Change]) -> Option<Change>;
//   pub fn rebase_change_set(changes: &[Change], competing: &[&[Change]]) -> Vec<Change>;

/// Placeholder — not yet implemented.
pub fn placeholder() {}
