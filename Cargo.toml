[workspace]
members = [
    "logos-core",
    "logos-render",
    "logos-desktop",
    "logos-wasm",
    "logos-layout"
]
resolver = "2"

[profile.release]
lto = "fat"
codegen-units = 1
panic = "abort"
