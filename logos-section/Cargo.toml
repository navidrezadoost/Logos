[package]
name = "logos-section"
version = "0.1.0"
edition = "2021"
description = "Section container hierarchy, paradigm management, and queries for Logos"

[dependencies]
logos-core = { path = "../logos-core" }
uuid = { version = "1", features = ["v4", "serde"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
